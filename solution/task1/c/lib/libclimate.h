#if !defined(LIBCLIMATE_H)
#define LIBCLIMATE_H
    float gen_pressure(float );
    float gen_wind_speed(float );
    float gen_humidity(float , float );
    float temperature_generator();
    float dew_point_calculator(float );
    float relative_humidity_calculator();
    float longitude_generator();
#endif // LIBCLIMATE_H
